# ✅ TODO.md (MVP 필수 기능)

| 카테고리 | 작업 내용 |
|----------|-----------|
| 설정     | 글로벌 설정 파일 로딩 (`~/.yesman/yesman.yaml`) |
| 설정     | 프로젝트 설정 파일 로딩 (`./.yesman/yesman.yaml`) |
| 설정     | `mode: merge/local` 병합 로직 구현 |
| 실행     | `yesman claude` 명령어 처리 및 프로젝트 세션 실행 |
| 실행     | `projects.yaml` 기반 `tmux` 세션 및 패널 분할 구조 생성 |
| 자동입력 | 출력 정지 시간 1초 이상 시 선택 대기 판단 |
| 자동입력 | 패턴 디렉토리에서 텍스트 로딩 및 일치 검사 |
| 자동입력 | 매칭 시 자동 `"1"` 입력 처리 |
| 로깅     | `log_level` / `log_path` 적용 및 기본 로그 출력 |